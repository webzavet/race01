type: object
required:
  - data
properties:
    data:
      type: object
      required:
        - type
        - attributes
      properties:
        type:
          type: string
          enum: [cards_collection]
          description: The type of the resource.
        attributes:
          type: object
          required:
            - cards
          properties:
            cards:
              type: array
              description: List of cards in the collection.
              items:
                $ref: './CardData.yaml'
