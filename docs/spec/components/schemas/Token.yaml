type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - type
      - attributes
    properties:
      type:
        type: string
        enum: [ token ]
      attributes:
        type: object
        required:
          - token
        properties:
          token:
            type: string
            description: "Refresh Token"
